import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 0,
        maxWidth: 340,
        maxHeight: 530,
        alignContent: "space-around",
        paddingBottom: 20
    }
});
