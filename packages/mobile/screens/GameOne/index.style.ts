import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  parentContainer: {
    flex: 1,
  },
  gameEngineContainer: {
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 0,
  },
});
