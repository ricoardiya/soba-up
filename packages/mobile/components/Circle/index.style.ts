import {StyleSheet} from "react-native";

export default StyleSheet.create({
    box: {
        width: 50,
        height: 50,
        borderRadius: 50,
        borderWidth: 1,
        margin: 25,
        backgroundColor: "#fff"
    }
});